import 'package:flutter/material.dart';

class IconConstants {
  static const unFinishedIcon = Icon(Icons.brightness_1_outlined);
  static const finishedIcon = Icon(Icons.task_alt_rounded);
  static const floatingActionButtonIcon = Icon(Icons.add);
}
